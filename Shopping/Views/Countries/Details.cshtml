@model Shopping.Data.Entities.Country

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Country</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="AddState" asp-route-id="@Model.Id" class="btn btn-outline-primary">Add Department/State</a>
    <a asp-action="Eit" asp-route-id="@Model.Id" class="btn btn-outline-warning">Edit Country</a>|
    <a asp-action="Index" class=" btn btn-outline-success">Back to List</a>
</div>
